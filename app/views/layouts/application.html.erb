<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "logout"  %>
    <%end%>
    <p class="bg-cyan-500 text-center font-important notice" ><%= notice %></p>       
    <p class="bg-red-500 text-center font-important alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
